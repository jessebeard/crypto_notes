NOW
extract functions and components, add callback for unit tests.

SOON
convert to async promise structure,
create unit tests:
  > encodeMessage
  > decodeMessage
  > getKeyMaterial
  > getKey
  > encrypt
  > decrypt
  > handleSaveClick
  > handleGetClick
  >

make it so that password shows "****" but has a <(o)> button to see it.
add some real CSS.



convert App.jsx to async promise structure
add React Router with seperate pages for
  > encrypting or decrypting random messages
  > view notebook
  > add entry (secure) should use similar implementation of hide text as used for passwords

add some real CSS.
add proton and Oauth
work on integrating github & google (Sheets ideally)
add some real CSS.